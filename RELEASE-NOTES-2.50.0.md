# 2.50.0 发布说明

## 新增：设置页即时机翻
- 钩住 Obsidian 的 `Setting` 组件与常用控件组件（`ButtonComponent` / `DropdownComponent` / `TextComponent`）原型方法，在渲染时拦截每个字符串，用本插件已有的翻译引擎即时翻译 **其他插件的设置页文案**（设置项名称、描述、控件标签 / 占位符 / 下拉选项）。
- 路线：机翻优先，而非维护逐插件词典（i18n-plus 的众包词典对长描述、动态生成的设置页覆盖率为零，不适合）。
- 设置项：
  - 总开关（默认关，实时生效，联动钩子挂载 / 卸载）。
  - 翻译通道：腾讯翻译·免费（零配置）/ 百度机器翻译（需先配置 APP ID 与密钥）。
  - 不翻译的插件 ID 黑名单（逗号或空格分隔）。
  - 清空本地翻译缓存。
- 已是中文的设置页自动跳过（CJK 检测），顺带避免翻译本插件自身的中文 UI；首次打开有「原文→译文」短暂闪烁，之后走本地缓存无感。

## 修复：依赖安全
- `adm-zip` 传递依赖漏洞 GHSA-vwc7-r8mq-g2x9（解压跟随目标符号链接，可任意文件覆盖，medium）：通过 `pnpm.overrides` 将 `adm-zip` 从有漏洞的 `0.6.0` 抬到修复版 `0.6.1`（漏洞范围 `>= 0.5.9, <= 0.6.0`）。该依赖仅由 `onnxruntime-node` 在 Node 端使用，浏览器运行时不可达。

## 实现加固
- `setDesc` 的 HTML 安全翻译改为 DOM 内联文本节点遍历（逐节点改写 `nodeValue`），不再赋值 `element.innerHTML` / `outerHTML`，消除 XSS / 注入风险并保留 `<a>` / `<b>` 等标签结构。
- 原型补丁用 `bind` 绑定实例方法，避免 `this` 别名告警；`unbound-method` 处加 `eslint-disable` 注释。
